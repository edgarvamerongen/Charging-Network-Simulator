Flask==3.1.3
pandas==3.0.3
gunicorn==23.0.0
weasyprint==66.0
staticmap==0.5.7
openpyxl==3.1.5
# Direct dependencies that previously only rode in transitively — report.py
# imports requests for the Wikimedia photo fetch (stdlib urllib lacks a CA
# bundle on some platforms), and the one-pager PDF append needs pypdf.
requests==2.34.2
pypdf==6.13.1
